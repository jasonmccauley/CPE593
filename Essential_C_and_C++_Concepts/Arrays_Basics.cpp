// Arrays is defined as collection of similar data types
// Declaration of arrays is as follows
int A[5];

// Each location can be accessed
A[0] = 27;
A[1] = 10;

// Array will be created in the main memory if inside a function
// Declaration and initialization of array is as follows
int B[5] = {2, 9, 6, 8, 10};

// You can also access each element of array using for loop